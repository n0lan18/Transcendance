Launch program with Docker and "docker-compose up --build"
To see project on browser, go to https://localhost:8443
If want stop project (on linux) "CTRL + C"
If you want remove Docker containers "docker-compose down"

If you change frontend you can refresh to see changes.

